From a4c87c820fef714957232a1176c6c2fbffff15fb Mon Sep 17 00:00:00 2001
From: Kim Eik <kim@heldig.org>
Date: Tue, 8 May 2012 09:07:46 +0200
Subject: [PATCH 17/32] changed code formatting * added @since * added wacky
 mediawiki spacing * made methods public

Change-Id: I6e6305a72c610ea6f33a37b1adf503ece23e0ac5
---
 includes/Maps_Utils.php |   17 ++++++++++-------
 1 file changed, 10 insertions(+), 7 deletions(-)

diff --git a/includes/Maps_Utils.php b/includes/Maps_Utils.php
index ef7e0ec..dfc0361 100644
--- a/includes/Maps_Utils.php
+++ b/includes/Maps_Utils.php
@@ -7,9 +7,10 @@ class MapsUtils {
 	 * @static
 	 * @param $url
 	 * @return mixed
+	 * @since 1.1
 	 */
-	static function isValidURL($url){
-		return filter_var($url, FILTER_VALIDATE_URL,FILTER_FLAG_SCHEME_REQUIRED);
+	public static function isValidURL( $url ) {
+		return filter_var( $url , FILTER_VALIDATE_URL , FILTER_FLAG_SCHEME_REQUIRED );
 	}
 
 
@@ -18,10 +19,11 @@ class MapsUtils {
 	 * @static
 	 * @param $link
 	 * @return bool|string
+	 * @since 1.1
 	 */
-	static function isLinkParameter($link){
-		if(self::strStartsWith($link,'link:')){
-			return substr($link,5);
+	public static function isLinkParameter( $link ) {
+		if ( self::strStartsWith( $link , 'link:' ) ) {
+			return substr( $link , 5 );
 		}
 		return false;
 	}
@@ -32,8 +34,9 @@ class MapsUtils {
 	 * @param $haystack
 	 * @param $needle
 	 * @return bool
+	 * @since 1.1
 	 */
-	static function strStartsWith($haystack, $needle){
-		return strpos($haystack, $needle) === 0;
+	public static function strStartsWith( $haystack , $needle ) {
+		return strpos( $haystack , $needle ) === 0;
 	}
 }
\ No newline at end of file
-- 
1.7.9.5

